const initialState = {
    connected: false,
    player: {},
    onlinePlayers: []
};

const players = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default players;
